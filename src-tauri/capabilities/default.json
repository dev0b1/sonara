{
  "identifier": "default",
  "description": "Default permissions for Sonara",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "opener:default"
  ]
}
